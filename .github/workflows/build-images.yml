name: Build Docker Images

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: sample-app

jobs:
  build-sample-web:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up JDK 24
      uses: actions/setup-java@v4
      with:
        java-version: '24'
        distribution: 'temurin'

    - name: Cache Maven packages
      uses: actions/cache@v4
      with:
        path: ~/.m2
        key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
        restore-keys: ${{ runner.os }}-m2

    - name: Log in to Container Registry
      uses: docker/login-action@v3
      with:
        registry: ${{ env.REGISTRY }}
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: Extract metadata
      id: meta
      uses: docker/metadata-action@v5
      with:
        images: ${{ env.REGISTRY }}/${{ github.repository }}/sample-web
        tags: |
          type=ref,event=branch,suffix=-sample-web
          type=ref,event=pr,suffix=-sample-web
          type=raw,value=sample-web,enable={{is_default_branch}}

    - name: Build and push Docker image for sample-web
      run: |
        ./mvnw spring-boot:build-image \
          -pl sample-web \
          -DskipTests \
          -Dspring-boot.build-image.imageName=${{ env.REGISTRY }}/${{ github.repository }}/sample-web:latest \
          -Dspring-boot.build-image.publish=true

  build-sample-webflux:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up JDK 24
      uses: actions/setup-java@v4
      with:
        java-version: '24'
        distribution: 'temurin'

    - name: Cache Maven packages
      uses: actions/cache@v4
      with:
        path: ~/.m2
        key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
        restore-keys: ${{ runner.os }}-m2

    - name: Log in to Container Registry
      uses: docker/login-action@v3
      with:
        registry: ${{ env.REGISTRY }}
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: Extract metadata
      id: meta
      uses: docker/metadata-action@v5
      with:
        images: ${{ env.REGISTRY }}/${{ github.repository }}/sample-webflux
        tags: |
          type=ref,event=branch,suffix=-sample-webflux
          type=ref,event=pr,suffix=-sample-webflux
          type=raw,value=sample-webflux,enable={{is_default_branch}}

    - name: Build and push Docker image for sample-webflux
      run: |
        ./mvnw spring-boot:build-image \
          -pl sample-webflux \
          -DskipTests \
          -Dspring-boot.build-image.imageName=${{ env.REGISTRY }}/${{ github.repository }}/sample-webflux:latest \
          -Dspring-boot.build-image.publish=true
